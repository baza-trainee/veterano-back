server:
  port: 8081
spring:
  application:
    name: user
  datasource:
    username: dev
    password: password
    url: jdbc:postgresql://localhost:5432/veterano_local_db
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
